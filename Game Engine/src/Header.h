#pragma once
#include <iostream>

class ResourceManager {
public:
	ResourceManager() {}

	//	TODO: Get a list of all the objects within the scene and distribute them to an thread pool to spawn them all in faster. 
	//	Main difference being on loading big models


private:


};